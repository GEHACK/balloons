# Contest credentials.
.env

# Cargo / dx build output.
/target
/dist

# direnv's nix profile + GC roots.
/.direnv

# Local ticket-state database (STATE_DB).
*.db
*.db-shm
*.db-wal

# nix build symlink.
result

.DS_Store

# Local DOMjudge checkout (dev environment).
/.domjudge-src
